// Public Vars
const maxScore = 20;

// Public DOM Vars
const maxScoreContainer = document.querySelector('.max-score');

function gameInit() {
  maxScoreContainer.textContent = maxScore;
}

// Manipulating dom

// Main Invokation

gameInit();
